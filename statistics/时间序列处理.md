```python

import pandas as pd
import numpy as np
from datetime import datetime, timedelta

start_time = datetime(2023, 1, 1, 0, 0)  # 2023年1月1日上午9点
end_time = datetime(2023, 8, 1, 23, 59)   # 2023年1月1日下午4点
interval = timedelta(minutes=1)
time_index = pd.date_range(start=start_time, end=end_time, freq=interval)
simulated_data = range(1,len(time_index) + 1)  # 1440分钟对应一天
df = pd.DataFrame(data={'Value': simulated_data}, index=time_index)
print(df)

```
#### 按日期
```python
# 按照 时间单位 计算后整合成数据  range(1,len(time_index) + 1) #一小时 60-1 = 59 没毛病
df.resample('1H').apply(lambda x: x.iloc[-1] - x.iloc[0])

```


#### 按一天分割成N组
```python
daily_groups = df.groupby(pd.Grouper(freq='D'))
for _index, daily_data in daily_groups:
    print(daily_data)
    
    
```

####  将一个小时按分钟数分割成2份
```python
def split_data(hour):
    #不要大于 24
    return daily_data[:hour * 60],daily_data[hour * 60:]
```

####
```python
df["start"] = df["start"] / 1000
df['start'] = pd.to_datetime(df['start'], unit='s')

df.set_index('start', inplace=True)
df.rename_axis('datetime', inplace=True)
```


#### 白噪声

##### Ljung-Box检验


```python

# LB统计量小于选定置信水平下的临界值，或者
# 值大于显著性水平（如0.05），不能拒绝原假设，序列为白噪声；
# LB统计量大于选定置信水平下的临界值，或者
# 值小于显著性水平（如0.05），拒绝原假设，序列非白噪声；
# 延迟6阶、12阶时 p值较大，增加到延迟24阶时，p值略小但也大于0.05，所以在95%的置信水平下认为序列为白噪声。
# （但若延迟24阶时，p值小于0.05，则认为序列为非白噪声）
import numpy as np
import matplotlib.pyplot as plt
from statsmodels.stats.diagnostic import acorr_ljungbox

white_noise=np.random.standard_normal(size=100)

# 不再指定boxpierce参数，近返回QLB统计量检验结果
# 同时设置lags参数为一个列表，相应只返回对应延迟阶数的检验结果
res = acorr_ljungbox(white_noise, lags=[6,12,24], return_df=True)

print(res)
```
###### 自相关图

```python
import numpy as np
import matplotlib.pyplot as plt

# 生成随机数据
n = 1000
mean = 0
std_dev = 1
data = np.random.normal(mean, std_dev, n)

fig = plt.figure(figsize=(20, 10))
ax1 = fig.add_subplot(211)
ax2 = fig.add_subplot(223)
ax3 = fig.add_subplot(224)

ax1.plot(data)
ax1.set_title('white noise')
plot_acf(data, ax=ax2)
plot_pacf(data, ax=ax3)
plt.show()

```
#####   

```python
#  统计量小于选定置信水平下的临界值，或者
# 值大于显著性水平（如0.05），不能拒绝原假设，可认为序列为白噪声序列；

# 统计量大于选定置信水平下的临界值，或者 值小于显著性水平（如0.05），拒绝原假设，认为至少存在某一阶数的自相关，序列非白噪声。
# 只看BP检验统计量下检验效果，即最后两列。可以可看到，各延迟阶数下的p值均大于0.05，不能拒绝原假设，序列为白噪声。
import numpy as np
import matplotlib.pyplot as plt
from statsmodels.stats.diagnostic import acorr_ljungbox
# 随机种子
np.random.seed(123)
# 生成白噪声序列
white_noise=np.random.standard_normal(size=1000)
# 白噪声检验，想要查看QBP检验统计量的结果需要指定boxpierce参数为True
# 不指定boxpierce参数，默认返回QLB检验统计量的结果
res = acorr_ljungbox(white_noise, lags=24, boxpierce=True, return_df=True)
print(res)


```