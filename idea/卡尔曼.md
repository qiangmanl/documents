```python

import numpy as np
位移=速度 × 时间 + 1/2​ × 加速度 × 时间**2

# x0 [[位置],[​速度]] 任何你需要跟踪的信号
# 卡尔曼滤波假设x0两个变量（位置和速）都是随机的，并且服从高斯分布。每个变量都有一个均值 ，表示随机分布的中心（最可能的状态），以及方差 ，表示不确定性。
x0 = np.array([[0],
               [0]])


# 定义时间步长
delta_t = 1

# 定义 F 矩阵
F = np.array([[1, delta_t],
              [0, 1]])

# 定义 B 矩阵
B = np.array([[1/2 * delta_t**2],
              [delta_t]])
```